from django.apps import AppConfig


class UploadImageConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "recrutation_excercise.upload_image"  # solution by stack overflow
